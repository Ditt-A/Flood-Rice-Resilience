# Data Folder

Place the raw datasets in `data/raw/`. This package already includes the two allowed datasets used by the notebooks:

- `Flood Prediction.zip`
- `Rice Supply Chain in West Java Province, Indonesia.zip`

No external dataset is required or used for model training.
